import React from "react";
import ReactDOM from "react-dom";
import { Header } from "./Header";

const root = document.querySelector("#root");

ReactDOM.render(<Header text="Moja pierwsza strona w React" />, root);
